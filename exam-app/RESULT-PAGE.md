# 결과 페이지 (성적통지표) 명세 — 핸드오프

시험 제출 직후 보게 되는 화면. **이 콘텐츠에서 가장 오래 머무는 화면이자 유일한 공유 대상**이다.
목적은 두 가지 — ① 성적표 이미지가 SNS로 퍼져 책을 알린다 ② 오답을 통해 책 구매로 이어진다.

> 시험지 레이아웃 규칙은 [LAYOUT.md](./LAYOUT.md)를 본다. 이 문서는 **제출 이후 화면**만 다룬다.

---

## 현재 상태 (수정 대상)

| 파일 | 지금 | 이후 |
|------|------|------|
| `src/App.tsx` | `submitted` → `gradeExam()` → 헤더에 `34/50점` 표시, `viewMode: 'exam' \| 'answerKey'` | `viewMode`에 결과 화면 추가 |
| `src/grade.ts` | `{ earned, max }`만 반환 | 표준점수·백분위·등급·문항별 정오 추가 |
| `src/components/AnswerKeyView.tsx` | 별도 정답표 화면 (문항번호/정답/배점) | **흡수해서 제거** — 아래 «문항별 채점표»가 상위 집합 |
| `SheetHeaderFirst.tsx` | 성명·수험번호 칸이 **빈칸** | 응시 전 입력받은 이름/번호를 채운다 |

---

## 0. 응시 전 — 이름 입력

시험 시작 전에 **이름 한 칸만** 받는다. 수험번호는 응시 시각 기반으로 자동 발급한다 (예: `25010812`).

- 입력한 이름은 시험지 첫 장 헤더의 **성명란**(`SheetHeaderFirst` 셋째 줄)에 그대로 들어간다.
- 성적표에도 같은 이름·번호가 찍힌다. 시험지와 성적표가 한 세트로 보이게 하는 장치다.
- **손글씨 서체**로 렌더한다. 인쇄된 양식 위에 사람이 적어 넣은 느낌이 이 콘텐츠의 핵심 디테일이다.

---

## 1. 화면 구성

```
┌────────────────────────────────┐
│                                │
│        성적통지표 카드          │   ← 이미지로 저장되는 영역 (캡처 경계)
│                                │
└────────────────────────────────┘

         문항별 채점표 확인하기 ›        ← 팝업
         오답노트 확인하기 (7문항) ›     ← 팝업
         성적표 이미지로 저장 ›          ← 다운로드

        ┌──────────────────────┐
        │  📖 책에서 확인하기   │        ← 버튼 (외부 링크)
        └──────────────────────┘
```

- 성적표 카드 하나가 본문을 독점한다. 채점표·오답노트는 **전부 팝업**으로 뺀다.
- 앞의 세 개는 **텍스트 링크**, 마지막 책만 **버튼**이다. 색도 책만 포인트 컬러, 나머지는 먹색.
- **세로로 쌓고 가운데 정렬.** 성적표 폭 안에 두면 성적표 하단 안내문처럼 보여 톤이 맞는다.
  가로로 늘어놓지 않는다 — 세로로 긴 문서라 톤이 깨진다.
- 순서를 바꾸지 않는다: 확인 → 복습 → 저장 → 구매.

### 제출 → 성적표 전환

즉시 띄우지 말고 **`채점 중입니다` 3초** 후 성적표가 나타난다. 체감을 크게 올리는 싼 연출이다.

---

## 2. 성적통지표 카드

수능 성적통지표 양식을 그대로 재현한다. 이미지로 저장되는 유일한 영역이므로 **캡처 경계를 DOM에서 명확히** 잡는다.

```
┌──────────────────────────────────────────────┐
│        2026학년도 제미나이 활용 능력 시험      │
│                 성적통지표                    │
│                                              │
│  수험번호 25010812      성명 ㅇㅇㅇ (손글씨)   │
│  ┌────────┬──────┬────────┬──────┬────┐      │
│  │  영역  │원점수│표준점수│백분위│등급│      │
│  ├────────┼──────┼────────┼──────┼────┤      │
│  │제미나이│34/50 │  118   │  87  │ 2  │      │
│  │  활용  │      │        │      │    │      │
│  └────────┴──────┴────────┴──────┴────┘      │
│                                              │
│              2026년 8월 6일                   │
│         이지스퍼블리싱 ○○평가원  (印)         │
└──────────────────────────────────────────────┘
```

| 항목 | 내용 |
|------|------|
| 표 열 | 영역 / 원점수 / 표준점수 / 백분위 / 등급 |
| 강조 | **등급을 가장 크게.** 원점수보다 등급이 공유 욕구를 만든다 |
| 하단 | 시험 날짜(응시일) + 기관명 |
| 기관명 | **이지스퍼블리싱**을 평가원처럼 쓴다. 직인 도장 하나 |
| 비고란 | **부정행위 무효 도장이 찍힐 자리를 지금 비워둔다** (아래 §6) |

### 백분위 — 서버 기록 기반 실제 값

가상 분포를 쓰지 않는다. **응시 결과를 서버에 기록하고 실제 응시자 대비 백분위를 계산**한다.

- 표준점수·등급도 같은 분포에서 산출한다. 등급컷은 누적 비율(4%/11%/23%…) 기준이 자연스럽다.
- 응시자가 적은 초기에는 값이 튀므로 **최소 표본 미달 시 기준 분포로 대체**하는 처리가 필요하다.
- 카드 아래 한 줄로 `오늘 1,284명 응시 · 평균 31.2점`을 넣는다. 백분위가 진짜라는 근거가 되고,
  숫자가 클수록 공유율이 올라간다.

> 서버 스키마·API는 미정. 최소한 `{ 이름, 수험번호, 원점수, 문항별 정오, 응시시각 }`은 남긴다.

---

## 3. 팝업 ① 문항별 채점표

기존 `AnswerKeyView`(정답표)를 여기에 흡수한다. 정답표는 `문항/정답/배점`이고 채점표는 거기에
`내 답`과 `○✕`를 더한 것이라 **완전히 포함**된다. 별도 화면을 남기지 않는다.

### 축소 ↔ 확대

20문항 고정이므로 **4열 × 5행 그리드**. 축소·확대가 **같은 배치**를 유지하고 담기는 정보만 달라진다.
자리를 미리 잡아두면 확대가 레이아웃 변경이 아니라 **줌**처럼 느껴진다.

```
축소                        확대
┌───┬───┬───┬───┐          ┌──────────┬──────────┐
│1 ○│2 ✕│3 ○│4 ○│    →     │1 내③ 정③ │2 내① 정④│
│5 ○│6 ○│7 ✕│8 ○│          │  2점   ○ │  3점   ✕ │
│ …               │          │ …
```

| 규칙 | 값 |
|------|-----|
| 축소 상태 | 문항 번호 + `○`/`✕`만 |
| 확대 상태 | 내 답 / 정답 / 배점까지 |
| 미선택 | `—` 로 표시 — 찍어서 틀린 것과 못 푼 것을 구분한다 |
| 오답 칸 | 옅은 붉은 배경. 축소 상태에서도 동일 |
| 오답 칸 클릭 | **오답노트 팝업으로 전환** (겹쳐 띄우지 말고 갈아끼운다) |
| 열 수 | 문항 수가 20이 아니어도 **4열 고정**, 행만 늘린다 |
| 모바일 | 2열로 떨어뜨린다 |

---

## 4. 팝업 ② 오답노트

**한 문제씩 넘겨 본다.** 목록으로 쌓지 않는다.

```
┌──────────────────────────────┐
│  오답노트          2 / 7   ✕ │
├──────────────────────────────┤
│  7번  [3점]                  │
│  ┌────────────────────────┐  │
│  │  문항 전문 (시험지 그대로)│  │
│  └────────────────────────┘  │
│  내 답 ①   ·   정답 ④        │
│  ──────────────────────────  │
│  해설 두세 줄…                │
│  📖 3장 — 문서와 이미지 다루기 │
├──────────────────────────────┤
│  ‹ 이전   2·7·11·14·16   다음 ›│
└──────────────────────────────┘
```

- **문항 전문을 다시 보여준다.** 요약만 있으면 뭘 틀렸는지 기억나지 않는다.
- `QuestionBlock`을 그대로 재활용한다. 이 컴포넌트는 **단 폭 318px 기준**으로 만들어졌으므로
  **모달 본문 폭을 318px 근처로 잡으면 레이아웃이 깨지지 않는다.** (LAYOUT.md의 `columnWidth()`)
- 좌우 화살표 + 모바일 스와이프 + 키보드 `←` `→`, `ESC` 닫기.
- 하단 인디케이터는 점이 아니라 **틀린 문항 번호**로 둔다. 바로 점프할 수 있고 몇 번을 틀렸는지도 보인다.
- **마지막 장 다음에 한 장 더** — `7문항을 틀렸습니다. 이 내용은 책 3·5·7장에 있습니다` + 서점 버튼.
  넘기다 보면 자연스럽게 CTA에 도착한다. 가장 잘 눌릴 자리다.

### 필요한 데이터 (미확보)

문항마다 아래 두 개가 있어야 한다. `src/data/exam-sample.json`에 필드를 추가한다.

| 필드 | 내용 |
|------|------|
| `explanation` | 해설 2~3줄 |
| `source` | 책의 장·쪽수. **정확한 쪽수가 부담이면 장 제목까지만** 해도 효과는 거의 같다 |

이 안내가 있어야 책 버튼이 광고가 아니라 안내로 읽힌다. 20문항 전부 채워야 한다.

---

## 5. 액션

### 성적표 이미지로 저장
§2 카드 영역만 잘라 PNG로 내려받는다. 카드 안에 이미 기관명·책 정보가 박혀 있어
**이미지 자체가 광고물**이 된다. 사용자는 자랑하려고 저장하지만 결과적으로 책을 퍼뜨린다.

### 📖 책에서 확인하기 (버튼)
**이지스퍼블리싱 자사 책 소개 페이지**로 보낸다. 서점 선택은 거기서 하게 한다 —
목차·미리보기·예제 파일까지 붙일 수 있고 유입 추적도 한 곳에서 된다.
성적표에 이미 이지스퍼블리싱이 기관명으로 박혀 있어 톤도 맞는다.

> 최종 URL 미정. 자사 페이지 vs 서점 직링크는 확인 필요.

### 만점자 분기
- 오답노트 링크를 **숨기지 말고** 흐리게 죽이고 `틀린 문항이 없습니다`로 바꾼다.
  사라지면 레이아웃이 흔들리고, 만점자야말로 자랑하러 온 사람이라 한 마디 해주는 게 맞다.
- 오답노트 마지막 장도 `이미 이 책의 독자시군요 — 주변에 추천해 주세요`로 분기한다.

---

## 6. 예정 — 부정행위 감지 (아직 설계 전)

시험 중 탭 이탈을 감지해 경고하고, 누적되면 **성적표에 무효 처리 도장**을 찍는 기획이 예정되어 있다.

- 성적통지표 카드 **비고란에 자리를 비워둔다** — `※ 부정행위 2회 적발 — 이 성적은 무효 처리되었습니다`
- 무효 성적표는 **별도 디자인 분기**가 될 가능성이 높다. 만점보다 무효 처리가 더 많이 공유될 수 있다.
- 이 문서의 §2 카드 구조는 그 분기를 감당할 수 있게 잡는다.

---

## 7. 하지 않는 것

- 칭호·캐릭터 (검토했으나 제외)
- 다시 응시하기 / 재도전 (제외)
- "제미나이한테 물어보기" 프롬프트 복사 (제외)
- 채점표·오답노트를 **요약 카드**로 본문에 노출 (텍스트 링크만)
- 전체 정답표 별도 화면 유지 (채점표에 흡수)
- 뉴스레터 구독 / 명예의 전당 / 문항 신고

### 나중에 별도로 볼 것

- **1등급 이벤트 응모** — 1·2등급일 때만 뜨는 조건부 배너, 이메일 수집. 서버가 들어가면 붙이기 쉽다.
- 친구에게 도전장 (내 점수가 담긴 공유 링크)

---

## 8. 미결 사항

| 항목 | 내용 |
|------|------|
| 서점 링크 | 자사 소개 페이지 URL 확정 |
| 해설·쪽수 | 20문항 `explanation` / `source` 데이터 작성 |
| 서버 | 응시 기록 저장·백분위 계산 API, 표본 부족 시 대체 분포 |
| 등급컷 | 누적 비율 기준 확정 |
| 기관명 | `이지스퍼블리싱 ○○평가원`의 정확한 명칭 |
